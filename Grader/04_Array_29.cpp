#include <iostream>
#include <algorithm>
#include <cmath>

using namespace std;
int main()
{
    int count;
    cin >> count;
    int X1[count],Y1[count],X2[count],Y2[count];
    for (size_t i = 0; i < count; i++)
    {
        cin >> X1[i] >> Y1[i] >> X2[i] >> Y2[i];
    }
    int Area[count][count];
    for (size_t i = 0; i < count; i++)
    {
        Area[count] = 0;
        for (size_t j = i + 1; j < count; j++)
        {
            
        }
        













        if (Area > MaxOverlappingArea)
        {

        }
        
    }
}